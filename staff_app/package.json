{
  "name": "7ty-staff-app",
  "version": "1.0.0",
  "description": "7TY Staff Mobile Application",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "dev": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
